<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<section>
    <h1>Recommended Songs 🎶</h1>
    <ul>
        <% songs.forEach(song => { %>
         <li><a href="/songs/<%= song._id %>"><%= song.title %></a>
            - <%= song.artist %>
        </li>
        <% }) %>  
    </ul>
</section>

<%- include('../partials/footer') %>